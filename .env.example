MONGODB_URI=mongodb+srv://user:pass@cluster0.xxxxx.mongodb.net/kareers
# openssl rand -base64 48
SESSION_SECRET=
# Vercel sends this as a Bearer token to /api/cron/refresh
CRON_SECRET=
# Classic token, no scopes. Enables contribution calendar + 5000 req/h
GITHUB_TOKEN=
# Used by `npm run seed`
ADMIN_EMAIL=placements@kluniversity.in
ADMIN_PASSWORD=
# Password reset emails (https://resend.com, free tier). Verify kareers.klef.me there and add its DNS records in Cloudflare.
RESEND_API_KEY=
MAIL_FROM=Kareers <no-reply@kareers.klef.me>
APP_URL=https://kareers.klef.me
